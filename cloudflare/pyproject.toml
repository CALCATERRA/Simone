[project]
name = "simone-cloudflare"
version = "0.1.0"
description = "Simone Instagram chatbot running on Cloudflare Workers"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]

[tool.pywrangler.assets]
"src/prompt.json" = "prompt.json"

